using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class newCheat : MonoBehaviour
{
    public GameManagerScript game;
    // Start is called before the first frame update
    void Start()
    {
        game.SkipLogo();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
